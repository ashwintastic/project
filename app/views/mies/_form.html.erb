<%= form_for(@my) do |f| %>
  <% if @my.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my.errors.count, "error") %> prohibited this my from being saved:</h2>

      <ul>
      <% @my.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
